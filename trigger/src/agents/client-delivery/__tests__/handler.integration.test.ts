import { describe, it } from "vitest";

describe.skip("client-delivery integration", () => {
  it.todo("generates real runbook via LLM");
  it.todo("generates real handoff doc via LLM");
  it.todo("generates real demo script via LLM");
  it.todo("writes all three files to correct paths");
  it.todo("calls requestApproval with correct payload");
});
