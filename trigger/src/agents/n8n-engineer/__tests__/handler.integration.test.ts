import { describe, it } from "vitest";

describe.skip("n8n-engineer integration", () => {
  it.todo("generates real workflow via LLM — email automation type");
  it.todo("generates real workflow via LLM — data sync type");
  it.todo("generates real workflow via LLM — AI processing type");
  it.todo("validate generated JSON with MCP n8n_validate_workflow");
});
